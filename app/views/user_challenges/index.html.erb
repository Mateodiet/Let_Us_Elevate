<div class="container font-size-chall">
    <div class="trans-back">
      <h3> Voici les challenges que nous te recommandons d'après ton design humain !</h3>
    </div>
        <% @userchallenges.each do |challenge| %>
      <div class="trans-back m-3 challenge-content p-2">
        <%= link_to challenge.challenge.title, user_challenge_path(challenge), class:"text-decoration-none text-white f-size-index" %>
      </div>
        <% end %>
</div>
